# Teletraan - Backend Environment Variables
# Copy this file to .env and configure as needed:
#   cp .env.example .env

# =============================================================================
# Database
# =============================================================================
DATABASE_URL=sqlite+aiosqlite:///./data/market-analyzer.db

# =============================================================================
# LLM Provider Configuration
# =============================================================================
# The app uses claude-agent-sdk for all LLM calls. The SDK reads authentication
# from environment variables. Configure exactly ONE of the options below.
#
# IMPORTANT (Anthropic TOS): Unless previously approved, Anthropic does not
# allow third-party developers to offer claude.ai login or rate limits for
# their products. Use an API key or cloud provider for production deployments.

# --- Option 1: Anthropic API Key (RECOMMENDED for production) ---
# Direct API access, pay-as-you-go pricing.
# Get your key at: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-api03-...

# --- Option 2: Amazon Bedrock ---
# Requires AWS credentials (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY or IAM role).
# CLAUDE_CODE_USE_BEDROCK=1
# AWS_REGION=us-east-1

# --- Option 3: Google Vertex AI ---
# Requires GCP credentials (GOOGLE_APPLICATION_CREDENTIALS or gcloud auth).
# CLAUDE_CODE_USE_VERTEX=1
# VERTEX_PROJECT=my-gcp-project
# VERTEX_REGION=us-central1

# --- Option 4: Azure AI Foundry ---
# Requires Azure credentials.
# CLAUDE_CODE_USE_FOUNDRY=1

# --- Option 5: Claude Code Subscription (default, dev-only) ---
# If none of the above are set, the SDK falls back to the local Claude Code
# subscription. This requires 'claude' CLI to be logged in on the host.
# NOT recommended for production or distributed deployments.

# --- Model override (optional) ---
# Default: claude-sonnet-4-20250514
# ANTHROPIC_MODEL=claude-sonnet-4-20250514

# --- Provider auto-detection override (optional) ---
# By default the app auto-detects the provider from the env vars above.
# You can force a specific provider: anthropic_api | bedrock | vertex | azure | subscription
# LLM_PROVIDER=auto

# =============================================================================
# Data Source API Keys (optional)
# =============================================================================
# FRED economic data (https://fred.stlouisfed.org/docs/api/api_key.html)
# FRED_API_KEY=your-fred-api-key

# Finnhub stock data (https://finnhub.io/)
# FINNHUB_API_KEY=your-finnhub-api-key

# =============================================================================
# Application Settings
# =============================================================================
# DEBUG=false
# PREDICTION_MARKETS_ENABLED=true
# REDDIT_SENTIMENT_ENABLED=true

# =============================================================================
# GitHub Pages Publishing (optional)
# =============================================================================
# Publishing is DISABLED by default for fork safety.
# GITHUB_PAGES_ENABLED=false
# GITHUB_PAGES_REPO=username/repo
# GITHUB_PAGES_BRANCH=gh-pages
# GITHUB_PAGES_BASE_URL=https://username.github.io/repo
